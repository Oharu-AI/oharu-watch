# プロジェクト移動のお知らせ（CODEX 共同開発向け）

このプロジェクト「おはるWATCH」は、CODEX と Claude で共同開発できるよう
次の場所へ移動しました。

現在の場所:

```
/Users/oharu/haru-ai-workspace/projects/oharu-watch
```

以前の場所（もう使いません）:

```
/Users/oharu/プロジェクト/おはるWATCH
```

今後 CODEX や Claude Code で作業するときは、必ず上の
`haru-ai-workspace/projects/oharu-watch` を開いてください。

## メモ

- フォルダー名を GitHub リポジトリ名（`Oharu-AI/oharu-watch`）と揃えて
  ASCII の `oharu-watch` にしました。日本語・濁点合成文字によるパスの不具合を避けるためです。
- Git リポジトリ（`.git`）・コミット履歴・リモート設定はそのまま引き継いでいます。
- リモート: `https://github.com/Oharu-AI/oharu-watch.git`（main へ直 push 運用）。

更新日: 2026-07-22
